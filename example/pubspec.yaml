name: example
description: A sample command-line application to show the usage of `carv_generators`.
version: 1.0.0

environment:
  sdk: '>=2.19.1 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build_runner: ^2.3.3
  carv_generators: ^1.0.0

dependencies:
  carv_annotations: ^1.0.0
